# Default policy set. Version-controlled, same branch-protection as application code (Phase 1, Section 1: Security).
roles:
  odoo_agent:
    - action: "odoo.read_orm"
      resource: "*"
      effect: allow
    - action: "odoo.explain_rule"
      resource: "*"
      effect: allow
    - action: "odoo.propose_change"
      resource: "*"
      effect: require_approval
    - action: "odoo.write_orm"
      resource: "*"
      effect: deny
    - action: "odoo.execute_migration"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    # Phase 11: raw function/class bodies are confidential by default
    # (Code Analysis Engine's classifier.py) — require_approval here,
    # never allow, regardless of how low-risk the requesting agent's own
    # other actions are. The structural tier (signatures, docstrings,
    # call graph) needs no rule at all: it's classified internal and
    # freely retrievable the same way any other Phase 9 content is.
    - action: "code_analysis.raw_source_request"
      resource: "*"
      effect: require_approval
    # Phase 12: MCP Client is a new tool SOURCE, not a new trust boundary
    # (doc, MCP Client Security) — allow here mirrors shell.execute's
    # existing precedent: the individual server was already approval-gated
    # at registration time, this is authorize-checked defense-in-depth on
    # each call, not a second human approval per invocation.
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    # Phase 6: execution layer actions. All "allow" rather than
    # require_approval again here — the human approval already happened
    # once, upstream, on odoo.propose_change itself (Phase 5). Shell
    # Executor and Git Manager still enforce their own allow-lists and
    # structural branch protection; this policy layer isn't the only
    # gate, just the one that would otherwise re-prompt a human for
    # something they already approved (Phase 6 doc, Section 3 end-to-end
    # diagram shows exactly one approval step, not one per git command).
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  database_agent:
    - action: "db.read"
      resource: "*"
      effect: allow
    - action: "db.dry_run"
      resource: "*"
      effect: allow
    # Both write paths require approval unconditionally at this layer —
    # not conditioned on the dry-run's impact estimate, which the policy
    # engine has no visibility into. "Above trivial impact" (Phase 7 doc)
    # is Database Agent's own job to judge when deciding whether to even
    # request db.propose_write; this policy layer stays maximally
    # conservative regardless, same posture as odoo.propose_change.
    # db.propose_migration in particular the doc calls out as a hard
    # rule that "cannot be granted to any capability regardless of how
    # it's declared" — never allow, ever.
    - action: "db.propose_write"
      resource: "*"
      effect: require_approval
    - action: "db.propose_migration"
      resource: "*"
      effect: require_approval
    # Connector-level execution actions — distinct from the propose_*
    # actions above, same split as Phase 6's git.push vs
    # odoo.propose_change. Allow here, because Database Connector's own
    # /db/write and /db/migrate are only ever reached after the matching
    # db.propose_write / db.propose_migration was already approved
    # upstream and Reasoning Engine's resume() is executing that
    # decision — not a second, independent request for the same action.
    - action: "db.write"
      resource: "*"
      effect: allow
    - action: "db.migrate"
      resource: "*"
      effect: allow
    - action: "db.write_direct"
      resource: "*"
      effect: deny
    - action: "db.ddl_direct"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "code_analysis.raw_source_request"
      resource: "*"
      effect: require_approval
    # Phase 12: MCP Client is a new tool SOURCE, not a new trust boundary
    # (doc, MCP Client Security) — allow here mirrors shell.execute's
    # existing precedent: the individual server was already approval-gated
    # at registration time, this is authorize-checked defense-in-depth on
    # each call, not a second human approval per invocation.
    - action: "mcp.invoke"
      resource: "*"
      effect: allow

  planner:
    - action: "planner.plan"
      resource: "*"
      effect: allow
    - action: "planner.replan"
      resource: "*"
      effect: allow
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    # Capability Registry (Phase 8) actions — planner is also the actor
    # for the registry's own startup sync from the agents service.
    # Registering a genuinely NEW capability is allow (it's just
    # reflecting an agent that's already shipped and code-reviewed);
    # CHANGING an existing capability's already-active scope, or
    # deprecating one, is the security-relevant event the Phase 8 doc
    # calls out ("routes through the same approval gate as Phase 2's
    # security-tagged config changes") — require_approval, not allow.
    - action: "capability.register_new"
      resource: "*"
      effect: allow
    - action: "capability.change_scope"
      resource: "*"
      effect: require_approval
    - action: "capability.deprecate"
      resource: "*"
      effect: require_approval

  django_agent:
    - action: "django.explain_structure"
      resource: "*"
      effect: allow
    - action: "django.propose_migration"
      resource: "*"
      effect: require_approval
    - action: "django.propose_config_change"
      resource: "*"
      effect: require_approval
    - action: "django.direct_deploy"
      resource: "*"
      effect: deny
    - action: "django.write_migration_direct"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "code_analysis.raw_source_request"
      resource: "*"
      effect: require_approval
    # Phase 12: MCP Client is a new tool SOURCE, not a new trust boundary
    # (doc, MCP Client Security) — allow here mirrors shell.execute's
    # existing precedent: the individual server was already approval-gated
    # at registration time, this is authorize-checked defense-in-depth on
    # each call, not a second human approval per invocation.
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    # django.propose_migration executes via the same migration_adapter/
    # django.py path Phase 7's Database Connector already stubbed — allow
    # here, same split as database_agent's db.migrate: the approval
    # already happened upstream on the propose_* action itself.
    - action: "db.migrate"
      resource: "*"
      effect: allow

  devops_agent:
    - action: "devops.explain_topology"
      resource: "*"
      effect: allow
    - action: "devops.propose_pipeline_change"
      resource: "*"
      effect: require_approval
    - action: "devops.propose_infra_change"
      resource: "*"
      effect: require_approval
    - action: "devops.execute_deploy"
      resource: "*"
      effect: deny
    - action: "devops.direct_infra_change"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "code_analysis.raw_source_request"
      resource: "*"
      effect: require_approval
    # Phase 12: MCP Client is a new tool SOURCE, not a new trust boundary
    # (doc, MCP Client Security) — allow here mirrors shell.execute's
    # existing precedent: the individual server was already approval-gated
    # at registration time, this is authorize-checked defense-in-depth on
    # each call, not a second human approval per invocation.
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    # Both propose_* actions land as a Git Manager MR (CI config / infra-as-code
    # files) — same execution-layer split as odoo_agent above: the human
    # approval already happened on the propose_* action itself. Git Manager's
    # branch/commit/push all route through Shell Executor internally, which
    # re-checks shell.execute for this SAME capability — omitting it here
    # denies every git.* call even though git.* itself is allowed (caught by
    # live testing, same class of gap odoo_agent's block already accounts for).
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  docker_agent:
    # Read-only inspection via Shell Executor — Phase 6's read-only
    # bypass rule applies here: no approval needed, Shell Executor's own
    # allow-list still confines this to docker ps/logs/inspect patterns.
    - action: "docker.inspect"
      resource: "*"
      effect: allow
    - action: "docker.propose_compose_change"
      resource: "*"
      effect: require_approval
    - action: "docker.exec_into_container"
      resource: "*"
      effect: deny
    - action: "docker.stop_prod"
      resource: "*"
      effect: deny
    - action: "docker.rm"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "code_analysis.raw_source_request"
      resource: "*"
      effect: require_approval
    # Phase 12: MCP Client is a new tool SOURCE, not a new trust boundary
    # (doc, MCP Client Security) — allow here mirrors shell.execute's
    # existing precedent: the individual server was already approval-gated
    # at registration time, this is authorize-checked defense-in-depth on
    # each call, not a second human approval per invocation.
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  testing_agent:
    # No approval on run_suite itself — it's read-only against a sandbox
    # with no real-world mutating effect. The environment constraint is
    # enforced structurally by Security Layer's /security/verify_environment
    # before every run, not by this policy layer (Phase 10 doc, Section 5).
    - action: "testing.run_suite"
      resource: "*"
      effect: allow
    - action: "testing.propose_new_test"
      resource: "*"
      effect: require_approval
    - action: "testing.report_coverage"
      resource: "*"
      effect: allow
    - action: "testing.run_against_prod"
      resource: "*"
      effect: deny
    - action: "testing.direct_ci_change"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "code_analysis.raw_source_request"
      resource: "*"
      effect: require_approval
    # Phase 12: MCP Client is a new tool SOURCE, not a new trust boundary
    # (doc, MCP Client Security) — allow here mirrors shell.execute's
    # existing precedent: the individual server was already approval-gated
    # at registration time, this is authorize-checked defense-in-depth on
    # each call, not a second human approval per invocation.
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  costing_agent:
    - action: "costing.calculate"
      resource: "*"
      effect: allow
    - action: "costing.explain_formula"
      resource: "*"
      effect: allow
    - action: "costing.propose_formula_change"
      resource: "*"
      effect: require_approval
    - action: "costing.modify_formula_direct"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow

  accounting_agent:
    # The most conservative role in this file by design (Phase 14 doc):
    # accounting.propose_entry requires approval unconditionally, and
    # there is no db.write allow here at all — an approved proposal
    # materializes as a real, reviewable git document (execution_bridge,
    # same as odoo.propose_change), never a direct ledger write. Real
    # ledger READS still need db.read (the tool-call step
    # accounting.read_ledger's template uses), so that alone is allowed.
    - action: "accounting.read_ledger"
      resource: "*"
      effect: allow
    - action: "accounting.explain_entry"
      resource: "*"
      effect: allow
    - action: "accounting.propose_entry"
      resource: "*"
      effect: require_approval
    - action: "accounting.write_ledger_direct"
      resource: "*"
      effect: deny
    - action: "accounting.close_period"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    - action: "db.read"
      resource: "*"
      effect: allow
    # accounting.propose_entry materializes via Git Manager (same split
    # as odoo_agent above) — shell.execute needed for the same reason
    # devops_agent's role docs this: Git Manager's own branch/commit/push
    # calls re-check shell.execute for this SAME capability.
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  inventory_agent:
    - action: "inventory.read_stock"
      resource: "*"
      effect: allow
    - action: "inventory.propose_adjustment"
      resource: "*"
      effect: require_approval
    - action: "inventory.propose_reorder"
      resource: "*"
      effect: require_approval
    - action: "inventory.write_stock_direct"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    # Reuses database_agent's exact dry-run-then-write path
    # (database_bridge.materialize_propose_write) — same three connector-
    # level actions that role already needed, for the same reason: the
    # capability-level approval already happened on inventory.propose_adjustment/
    # inventory.propose_reorder itself.
    - action: "db.read"
      resource: "*"
      effect: allow
    - action: "db.dry_run"
      resource: "*"
      effect: allow
    - action: "db.write"
      resource: "*"
      effect: allow

  manufacturing_agent:
    - action: "manufacturing.explain_workflow"
      resource: "*"
      effect: allow
    - action: "manufacturing.flag_constraint"
      resource: "*"
      effect: allow
    - action: "manufacturing.propose_schedule_change"
      resource: "*"
      effect: require_approval
    - action: "manufacturing.execute_schedule_direct"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    # manufacturing.flag_constraint's real stock/capacity check needs
    # db.read (same tool-call shape database_agent's own role already
    # established); propose_schedule_change materializes via Git Manager,
    # same split as odoo_agent/accounting_agent above — shell.execute
    # needed because Git Manager's branch/commit/push re-check it for
    # this SAME capability.
    - action: "db.read"
      resource: "*"
      effect: allow
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  sales_agent:
    - action: "sales.explain_status"
      resource: "*"
      effect: allow
    - action: "sales.propose_quote"
      resource: "*"
      effect: require_approval
    - action: "sales.propose_order_change"
      resource: "*"
      effect: require_approval
    - action: "sales.execute_order_direct"
      resource: "*"
      effect: deny
    - action: "sales.access_full_customer_pii_unscoped"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    - action: "db.read"
      resource: "*"
      effect: allow
    # Phase 15: PII is an orthogonal dimension to db.read itself (see
    # Database Connector's scoping.py) — this role check is the first,
    # coarse gate ("is sales_agent allowed to request PII fields at all"),
    # separate from and in addition to the target-specific
    # authorized_capabilities allow-list Database Connector's own
    # pii_registry.yaml enforces per column.
    - action: "db.read_pii"
      resource: "*"
      effect: allow
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  project_management_agent:
    - action: "pm.explain_status"
      resource: "*"
      effect: allow
    - action: "pm.flag_at_risk"
      resource: "*"
      effect: allow
    - action: "pm.propose_milestone_update"
      resource: "*"
      effect: require_approval
    - action: "pm.close_project_direct"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    # pm.propose_milestone_update materializes via Git Manager, same
    # split as every other propose_* action in this file — its
    # task.read tool call itself goes straight to platform-spine's
    # Gateway over HTTP (Phase 15's task_bridge.py), not through this
    # policy layer at all, the same bypass-by-design every other
    # tool-call action already uses.
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  code_review_agent:
    - action: "review.fetch_diff"
      resource: "*"
      effect: allow
    - action: "review.check_callers"
      resource: "*"
      effect: allow
    # Neither finalizing action requires its own human approval (Phase
    # 16 doc: "its own output is advisory, feeding INTO approval rather
    # than bypassing it") — the assessment attaches to ANOTHER agent's
    # pending approval via the new attach_review mechanism, it's never
    # itself a decision this policy layer needs to gate.
    - action: "review.flag_concern"
      resource: "*"
      effect: allow
    - action: "review.approve_recommendation"
      resource: "*"
      effect: allow
    - action: "review.merge"
      resource: "*"
      effect: deny
    - action: "review.override_human_approval"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    # review.fetch_diff calls Git Manager's real, read-only /git/diff
    # (Phase 6) — shell.execute needed for the same reason every other
    # git-touching role documents it: Git Manager's own call re-checks
    # shell.execute for this SAME capability at the Shell Executor layer.
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "shell.execute"
      resource: "*"
      effect: allow

  reverse_engineering_agent:
    - action: "reverse_eng.explain_undocumented"
      resource: "*"
      effect: allow
    - action: "reverse_eng.propose_documentation_draft"
      resource: "*"
      effect: require_approval
    - action: "reverse_eng.modify_code_direct"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    # An approved propose_documentation_draft materializes via Git
    # Manager first (same split as every other propose_* action), THEN
    # chains into Documentation Engine's real POST /docs/ingest (Phase 9)
    # — the first agent capability besides human_admin's wildcard to get
    # this grant, since no agent needed to write real documentation
    # before this phase.
    - action: "docs.ingest"
      resource: "*"
      effect: allow
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  architecture_agent:
    - action: "architecture.explain_existing"
      resource: "*"
      effect: allow
    - action: "architecture.propose_decision"
      resource: "*"
      effect: require_approval
    - action: "architecture.implement_direct"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  # Phase 11: the actor for Git Manager's own best-effort auto-trigger of
  # an incremental scan after a commit lands (execution/git_manager/api.py)
  # — a system action, not an agent capability, so it gets its own minimal
  # role rather than piggybacking on human_admin's blanket allow.
  calculation_agent:
    - action: "calc.apply_formula"
      resource: "*"
      effect: allow
    - action: "calc.explain_formula"
      resource: "*"
      effect: allow
    - action: "calc.assert_unverified_number"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    # calc.apply_formula's real evaluation runs via Shell Executor's
    # sandbox (eval_formula.py, Phase 17) — shell.execute needed for the
    # same reason every sandboxed-tool-call role documents it. No git.*
    # at all: this agent has no propose_* action, nothing ever
    # materializes via Git Manager.
    - action: "shell.execute"
      resource: "*"
      effect: allow

  cutlist_optimization_agent:
    - action: "cutlist.gather_parameters"
      resource: "*"
      effect: allow
    - action: "cutlist.explain_result"
      resource: "*"
      effect: allow
    # The tool-call itself bypasses this check by design (Phase 7's
    # established tool-call mechanism) — this require_approval rule only
    # ever fires on the FINALIZING turn (stock_length left empty, the
    # model reporting its already-real result), per the Phase 17 doc's
    # own scope decision to keep this unconditionally approval-gated
    # rather than try to structurally detect "feeds a real schedule
    # change" from the call alone.
    - action: "cutlist.run_optimizer"
      resource: "*"
      effect: require_approval
    - action: "cutlist.generate_layout_direct"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    - action: "shell.execute"
      resource: "*"
      effect: allow

  autocad_agent:
    - action: "autocad.explain_drawing"
      resource: "*"
      effect: allow
    - action: "autocad.propose_annotation"
      resource: "*"
      effect: require_approval
    - action: "autocad.modify_drawing_direct"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    # shell.execute covers both real needs: dxf_parse.py's sandboxed run
    # AND propose_annotation's git materialization re-checking it at the
    # Shell Executor layer.
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  python_agent:
    - action: "python.explain_code"
      resource: "*"
      effect: allow
    # python.propose_script is a suggestion in the response text itself,
    # not a materializing action — no approval, no git.* needed for it
    # specifically (Phase 18 doc, Section 1: distinct from propose_change).
    - action: "python.propose_script"
      resource: "*"
      effect: allow
    - action: "python.propose_change"
      resource: "*"
      effect: require_approval
    - action: "python.execute_direct"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  documentation_agent:
    - action: "docs.answer_from_existing"
      resource: "*"
      effect: allow
    - action: "docs.propose_new_doc"
      resource: "*"
      effect: require_approval
    - action: "docs.publish_direct"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    # An approved docs.propose_new_doc chains into the same real
    # POST /docs/ingest reverse_engineering_agent already uses (Phase 16)
    # — same grant, reused for a second agent.
    - action: "docs.ingest"
      resource: "*"
      effect: allow
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  security_agent:
    - action: "security.review_change"
      resource: "*"
      effect: allow
    - action: "security.explain_risk"
      resource: "*"
      effect: allow
    # A pure HTTP read against governance's own /audit/query — never
    # routes through Shell Executor, so no shell.execute/git.* needed
    # anywhere in this role at all, unlike every propose_*-capable agent.
    - action: "security.audit_query"
      resource: "*"
      effect: allow
    - action: "security.modify_policy_direct"
      resource: "*"
      effect: deny
    - action: "security.grant_permission"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow

  research_agent:
    - action: "research.synthesize_internal"
      resource: "*"
      effect: allow
    - action: "research.propose_external_lookup"
      resource: "*"
      effect: require_approval
    - action: "research.invoke_mcp_tool"
      resource: "*"
      effect: allow
    - action: "research.access_external_direct"
      resource: "*"
      effect: deny
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "mcp.invoke"
      resource: "*"
      effect: allow
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  coding_agent_gateway:
    # Phase 22: the one action this whole role exists for — always
    # require_approval, never allow, since an approved run still only
    # proceeds if coding_gateway_bridge.py's own sandbox-backend check
    # passes (structural, enforced in code, not by this policy layer).
    - action: "coding_gateway.propose_run"
      resource: "*"
      effect: require_approval
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "task.read"
      resource: "*"
      effect: allow
    - action: "task.update_status"
      resource: "*"
      effect: allow
    - action: "shell.execute"
      resource: "*"
      effect: allow
    - action: "git.branch"
      resource: "*"
      effect: allow
    - action: "git.commit"
      resource: "*"
      effect: allow
    - action: "git.diff"
      resource: "*"
      effect: allow
    - action: "git.push"
      resource: "*"
      effect: allow
    - action: "git.open_mr"
      resource: "*"
      effect: allow

  git_manager:
    - action: "code_analysis.scan"
      resource: "*"
      effect: allow

  mcp_surface:
    # Phase 26: real actions for the 8 real MCP tools this role exists
    # for — every one "allow", every one still authorize()+audit_log()
    # gated per call, same defense-in-depth posture as mcp.invoke's own
    # role grants since Phase 12. Deliberately NO
    # "approval.decide"-shaped action anywhere in this role — an
    # AI-driven IDE session must never be able to approve its own risky
    # actions (Phase 26 doc, Section 0's own explicit requirement).
    # Structural, not just an omission: there is no tool in
    # services/mcp-surface/ that could even call one.
    - action: "mcp_surface.submit_task"
      resource: "*"
      effect: allow
    # Gateway's own POST /api/v1/tasks independently re-checks
    # "task.create" for whatever actor a bearer token resolves to
    # (platform_spine/gateway/api.py) — the same layered-authorization
    # pattern every other Gateway-calling capability already needs
    # (e.g. odoo_agent's own task.create grant).
    - action: "task.create"
      resource: "*"
      effect: allow
    - action: "mcp_surface.get_task_status"
      resource: "*"
      effect: allow
    - action: "mcp_surface.ask_agent"
      resource: "*"
      effect: allow
    - action: "mcp_surface.search_knowledge"
      resource: "*"
      effect: allow
    - action: "mcp_surface.get_erp_schema"
      resource: "*"
      effect: allow
    - action: "mcp_surface.list_pending_approvals"
      resource: "*"
      effect: allow
    - action: "mcp_surface.get_audit_trail"
      resource: "*"
      effect: allow
    - action: "mcp_surface.list_capabilities"
      resource: "*"
      effect: allow

  ide_client:
    # Phase 27: the OpenAI-compatible shim's own fixed, stub-auth actor
    # (services/platform-spine/platform_spine/gateway/openai_shim.py) —
    # same stub-identity posture as mcp_surface (Phase 26) and Control
    # UI (Phase 24), real per-user auth deferred to Phase 31. The actual
    # "confidential content structurally barred from a lower-ceiling
    # model" gate is NOT this allow rule — that's a separate,
    # content-dependent check openai_shim.py runs against assembly's
    # real ceiling_for_model() before ever calling the model, on every
    # single request regardless of this grant.
    - action: "model.generate"
      resource: "*"
      effect: allow
    - action: "model.list"
      resource: "*"
      effect: allow

  human_admin:
    - action: "*"
      resource: "*"
      effect: allow
